# Gemini CLI 美食推荐适配

本仓库提供一个全球美食加权随机推荐能力。当用户在该目录下使用 Gemini CLI 并表达就餐意向时（如「推荐晚餐」「不知道吃什么」「想吃点辣的」），按以下流程响应。

## 工作流

### 1. 解析意图

从对话中提取参数（不传则忽略）：

- 数量（默认 3）
- 关键词（口味/食材/类型，必须在以下白名单内，逗号分隔）：
  - 口味：麻辣 / 辣 / 甜 / 酸 / 咸 / 鲜
  - 食材：海鲜 / 牛肉 / 羊肉 / 猪肉 / 鸡肉 / 素食
  - 类型：面食 / 米饭类 / 汤 / 甜品 / 烧烤 / 火锅 / 咖喱 / 汉堡披萨 / 饮品
- 价格档位：经济(≤20) / 实惠(21-50) / 中档(51-100) / 高档(101-200) / 奢华(>200)
- 菜系名（如「川菜」）或地区（如「亚洲」「欧洲」）
- 已推荐过的 ID（用于「换一批」）

**否定关键词不要翻转传入**：用户说「不要太辣」时，不传 `辣`，也不传任何关键词。

### 2. 调用推荐脚本

```bash
python3 ./scripts/recommender.py \
  --count <默认1> \
  --alternates <默认3> \
  --keywords "<可选>" \
  --price-tier "<可选>" \
  --cuisine "<可选>" \
  --geo "<可选>" \
  --exclude-ids "<可选>"
```

默认返回 1 道主选 + 3 道备选（互不重复）。输出为单行 JSON。

### 3. 解析与呈现

- `main` 非空：主选按「**菜名** ¥价格（菜系） — 描述」突出展示，`alternates` 列为备选
- `exhausted=true` 或 `main=null`：走兜底链
- `filters.relaxed=true`：告知用户原档位不足已扩大

### 4. 兜底链

1. 自动放宽（脚本已内置）
2. 降级过滤后重试（去价格 → 去菜系 → 去地区）
3. 调用 Gemini 内置的搜索能力查 `"<用户原话> 推荐 菜品"`
4. 基于常识直接推荐，**必须标注「这些菜不在本地知识库内」**

### 5. 「换一批」

用户接着说「换一批」时，把上一轮所有 `id` 加入 `--exclude-ids`，沿用其他参数重新调脚本。

## 关键约束

- 始终先查本地知识库
- 不要凭空编造菜名或价格
- 价格单位是 CNY

更详细的兜底说明见 `skills/recommend-dish/references/fallback-strategy.md`。
